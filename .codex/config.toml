# Codex MCP configuration — auto-generated by dev server at runtime.
# Start the dev server (npm run dev) to populate MCP server entries.

[features]
hooks = true

[hooks]

[[hooks.Stop]]

[[hooks.Stop.hooks]]
type = "command"
command = 'python3 "$(git rev-parse --show-toplevel)/.codex/hooks/journal.py"'
timeout = 30
statusMessage = "updating journal"

# --- IWER managed (do not edit) ---
[mcp_servers.iwsdk-runtime]
command = "node"
args = ["/Users/felixz/Projects/brush/node_modules/@iwsdk/cli/dist/cli.js", "mcp", "stdio"]

[mcp_servers.iwsdk-reference]
command = "node"
args = ["/Users/felixz/Projects/brush/node_modules/@iwsdk/reference/dist/cli.js"]

[mcp_servers.hzdb]
command = "npx"
args = ["@meta-quest/hzdb", "mcp", "server"]

# --- end IWER managed ---
